# -----------------------------------------------------------------------------
# Environment variables for Git operations
#
# INSTRUCTIONS:
# 1. Replace the placeholder values below with your actual GitHub username and
#    a Personal Access Token (PAT).
# 2. Make sure this file is sourced by your shell or the script that needs these
#    variables. The git_branch_helpers.sh script will do this automatically.
# 3. IMPORTANT: Add this file to your .gitignore to avoid committing it.
#    echo ".env" >> .gitignore
# -----------------------------------------------------------------------------

# Your GitHub username
GITHUB_USERNAME="your-username-here"

# Your GitHub Personal Access Token (PAT)
# This is used for authentication instead of a password.
# Make sure your token has the necessary permissions (e.g., 'repo' scope).
GITHUB_TOKEN="token_here"
